import { StyleSheet } from 'react-native';

// Estilos compartilhados da Atividade 10 — tema Cinema
export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#0F0F1A',
  },
});
